$(() => {
  // $.ajax({
  //   method: "GET",
  //   url: "/api/users"
  // }).done((users) => {
  //   for(user of users) {
  //     $("<div>").text(user.name).appendTo($("body"));
  //   }
  // });
  // $(".fa-trash-alt").click(function() {
  //   console.log(this.id);
  //   $.ajax({
  //     method: "POST",
  //     url: `/api/resources/delete/${this.id}`}).done();
  // });
});
